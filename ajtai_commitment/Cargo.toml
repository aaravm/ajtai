[package]
name = "ajtai_commitment"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
nalgebra = "0.33.0"
rand = "0.8"  # Random number generation for generating key-value pairs and randomness
